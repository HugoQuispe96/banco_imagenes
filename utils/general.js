const ruta_https = `https://${process.env.URL_SERVER}${process.env.PREFIJO}/images/`;
const ruta_http = `http://${process.env.URL_SERVER}${process.env.PREFIJO}/images/`;

module.exports = {
  ruta_https,
  ruta_http
};